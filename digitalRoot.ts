export const digitalRoot = (n: number): number => {
  let digits: number = n.toString().length;
  let root:number = 0;
  for (let i = 0; i < digits; i++) {
    let root +=;
    
  }
  console.log(digits);
  return digits;
};
digitalRoot(1);
