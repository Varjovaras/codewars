export class Kata {
  static spinWords(words: string) {
    const wordCount: number = words.trim().split(/\s+/).length;
    for (let i = 0; i < wordCount; i++) {
      //   const element = [i];
    }
  }
}
